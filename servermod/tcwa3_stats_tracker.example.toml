# TCWA3 Stats Tracker server extension config template.
# Copy this file to tcwa3_stats_tracker.toml beside the server extension binary before editing it.
# Never commit the real tcwa3_stats_tracker.toml.

[server]
server_key = "main-linux-01"

[http]
base_url = "https://stats.example.com"
api_token = "aat_arma_server_REPLACE_WITH_REAL_TOKEN"
timeout_ms = 5000
connect_timeout_ms = 1000
max_response_bytes = 1048576
verify_tls = true

[logging]
level = "info"

[queue]
enabled = true
queue_file = "tcwa3_stats_tracker_queue.ndjson"
queue_sent_file = "tcwa3_stats_tracker_queue.sent.ndjson"
dead_letter_file = "tcwa3_stats_tracker_queue.dead.ndjson"
results_file = "tcwa3_stats_tracker_queue.results.ndjson"
max_attempts = 25
flush_budget = 3
